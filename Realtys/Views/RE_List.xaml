<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Realtys List"
             x:Class="Realtys.Views.RE_List">
   

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="+"
                     Clicked="OnAddedClicked" />
    </ContentPage.ToolbarItems>

    <!--<ListView x:Name="listView"
              ItemSelected="OnCollectionViewItemSelected">
        <ListView.ItemTemplate>

            <DataTemplate>
                <Frame BackgroundColor="#6FA4EC" Padding="1" CornerRadius="0" BorderColor="Black">
                    <Label FontSize="20" Padding="30,24" TextColor="White">
                        <Label.FormattedText>
                            <FormattedString>
                                <FormattedString.Spans>
                                    <Span Text="{Binding Nazev} "/>
                                    <Span Text="&#10;"/>
                                    <Span Text="Čistý roční výnos: "/>
                                    <Span Text="{Binding cistyRocniVynos}" />
                                    <Span Text="&#10;"/>
                                    <Span Text="Detail" />
                                </FormattedString.Spans>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </Frame>
            </DataTemplate>

--><!--
            <TextCell Text="{Binding Nazev}"
                          TextColor="Black"
                          Detail="{Binding cistyRocniVynos}"
                          DetailColor="Green"/>
       --><!--         
            
        </ListView.ItemTemplate>
    </ListView>-->


    <CollectionView x:Name="listView"
                    SelectionMode="Single"
                    SelectionChanged="OnCollectionViewSelectionChanged"
                    >
        <CollectionView.ItemsLayout>
            <GridItemsLayout Span="1" Orientation="Vertical" />
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame BackgroundColor="#6FA4EC"  CornerRadius="0" BorderColor="Black">
                    <Label FontSize="20" Padding="30,24" TextColor="White">
                        <Label.FormattedText>
                            <FormattedString>
                                <FormattedString.Spans>
                                    <Span Text="{Binding Nazev} "/>
                                    <Span Text="&#10;"/>
                                    <Span Text="Čistý roční výnos: "/>
                                    <Span Text="{Binding cistyRocniVynos}" />
                                    <Span Text="&#10;"/>
                                    <Span Text="Pořizovací cena: " />
                                    <Span Text="{Binding porizovaciCena}" />
                                </FormattedString.Spans>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </Frame>                
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
 


</ContentPage>